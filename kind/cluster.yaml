# Generated by 30-kind-up.sh from .env — safe to regenerate.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: kagent-n8n
nodes:
  - role: control-plane
    extraPortMappings:
      # Publish the kagent controller A2A NodePort to the host so n8n
      # (Docker Compose) can reach it via host.docker.internal:30883.
      - containerPort: 30883
        hostPort: 30883
        protocol: TCP
